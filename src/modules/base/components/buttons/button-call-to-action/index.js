import template from './template.jade';
import './styles.scss';

export default() => ({
    restrict: 'E',
    template,
    scope: {
        
    },
    link: (attrs, scope, element) => {
 
    }
});